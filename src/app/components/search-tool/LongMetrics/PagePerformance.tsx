// components/search-tool/LongMetrics/PagePerformance.tsx
import React from "react";

/**
 * Componente que muestra el rendimiento de las páginas.
 * Aquí se pueden mostrar datos como total de clics, impresiones, CTR y posición promedio por página.
 */
export default function PagePerformance() {
  // Placeholder: Se espera que se implementen cálculos o se reciba data procesada para cada página.
  return (
    <div className="mb-6">
      <h2 className="text-xl font-bold mb-2">Rendimiento de Páginas</h2>
      <p>Este componente mostrará el rendimiento de las páginas, ordenadas por clics, impresiones y posición.</p>
    </div>
  );
}
